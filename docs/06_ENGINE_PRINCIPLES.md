Everything is an Entity.
Every action is an Event.
The engine owns the world state.
The LLM never changes the world directly.
Memories are created from events.
Characters evolve gradually.
Identity changes slowly; state changes frequently.
The engine decides what information the LLM receives.
The application is local-first.
The engine is provider-agnostic.
